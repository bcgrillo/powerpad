<?xml version="1.0" encoding="utf-8"?>
<local:EditorControl
    x:Class="PowerPad.WinUI.Components.Editors.ChatEditorControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PowerPad.WinUI.Components.Editors"
    xmlns:lcontrols="using:PowerPad.WinUI.Components.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:chat="using:PowerPad.WinUI.ViewModels.Chat"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    mc:Ignorable="d">
    <local:EditorControl.Resources>
        <local:ChatTemplateSelector
            x:Key="ChatTemplateSelector"
            AssistantTemplate="{StaticResource AssistantDataTemplate}"
            UserTemplate="{StaticResource UserDataTemplate}" />
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:StringVisibilityConverter x:Key="StringVisibilityConverter" />
        <converters:BoolNegationConverter x:Key="BoolNegationConverter" />
        <converters:CollectionVisibilityConverter x:Key="CollectionVisibilityConverter" />
        <DataTemplate x:Key="UserDataTemplate" x:DataType="chat:MessageViewModel">
            <StackPanel
                MaxWidth="700"
                MinWidth="135"
                Padding="12"
                HorizontalAlignment="Right"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="{StaticResource OverlayCornerRadius}"
                Spacing="8"
                Margin="0,8,0,0">
                <controls:MarkdownTextBlock
                    IsTextSelectionEnabled="True"
                    Text="{x:Bind Content, Mode=OneWay}"
                    Background="Transparent"
                    TextWrapping="Wrap" />
                <TextBlock
                    HorizontalAlignment="Right"
                    Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    Text="{x:Bind DateTime}" />
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="AssistantDataTemplate" x:DataType="chat:MessageViewModel">
            <Grid Margin="0,8,0,0">
                <StackPanel
                    MaxWidth="700"
                    MinWidth="135"
                    Padding="12"
                    HorizontalAlignment="Left"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="{StaticResource OverlayCornerRadius}"
                    Spacing="8"
                    MinHeight="70"
                    Visibility="{x:Bind ErrorMessage, Mode=OneWay, Converter={StaticResource StringVisibilityConverter}, ConverterParameter=True}">
                    <TextBlock
                        x:Name="LoadingText"
                        Visibility="{x:Bind Loading, Mode=OneWay}"
                        Text="{x:Bind LoadingMessage, Mode=OneWay}"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <Expander
                        Visibility="{x:Bind Reasoning, Mode=OneWay, Converter={StaticResource StringVisibilityConverter}}"
                        IsExpanded="{x:Bind Loading, Mode=OneWay}"
                        ExpandDirection="Down"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Header="Mostrar pensamiento"
                        HorizontalAlignment="Stretch"
                        Padding="8,4,8,16"
                        Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                        BorderBrush="{ThemeResource SubtleFillColorSecondaryBrush}" MinHeight="0">
                        <Expander.Resources>
                            <Thickness x:Key="ExpanderHeaderBorderThickness">0</Thickness>
                            <Thickness x:Key="ExpanderChevronMargin">16,0,0,0</Thickness>
                            <SolidColorBrush x:Key="ExpanderHeaderBackground" Color="{ThemeResource SubtleFillColorSecondary}" />
                        </Expander.Resources>
                        <TextBlock
                            Text="{x:Bind Reasoning, Mode=OneWay}"
                            FontStyle="Italic" IsTextSelectionEnabled="True"
                            TextWrapping="Wrap" />
                    </Expander>
                    <controls:MarkdownTextBlock
                        HorizontalAlignment="Left"
                        Background="Transparent"
                        IsTextSelectionEnabled="True"
                        Visibility="{x:Bind Loading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=True}"
                        Text="{x:Bind Content, Mode=OneWay}"
                        TextWrapping="Wrap" />
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock
                            Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                            Style="{StaticResource CaptionTextBlockStyle}"
                            Text="{x:Bind DateTime}" />
                        <HyperlinkButton Tag="{x:Bind}" Click="CopyButton_Click" ToolTipService.ToolTip="Copiar contenido del mensaje" Padding="0">
                            <FontIcon 
                                Glyph="&#xE8C8;" 
                                FontSize="{StaticResource CaptionTextBlockFontSize}" 
                                Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
                        </HyperlinkButton>
                    </StackPanel>
                </StackPanel>
                <InfoBar
                    IsOpen="True"
                    Visibility="{x:Bind ErrorMessage, Mode=OneWay, Converter={StaticResource StringVisibilityConverter}}"
                    IsClosable="False"
                    Severity="Error"
                    HorizontalAlignment="Stretch"
                    Message="{x:Bind ErrorMessage, Mode=OneWay}" />
            </Grid>
        </DataTemplate>
    </local:EditorControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Margin="36,24,36,12" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>
            <lcontrols:EditableTextBlock
                x:Name="EditableTextBlock"
                FontSize="27.5"
                FontWeight="SemiBold"
                ForcedForeground="{ThemeResource TextFillColorPrimaryBrush}"
                Value="{x:Bind _document.Name, Mode=OneWay}"
                Edited="EditableTextBlock_Edited"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                <Button
                    x:Name="UndoButton"
                    ToolTipService.ToolTip="Eliminar último mensaje"
                    Click="UndoButton_Click"
                    Visibility="Collapsed"
                    Padding="8" BorderThickness="0">
                    <Button.Content>
                        <FontIcon FontSize="18" Glyph="&#xE845;" />
                    </Button.Content>
                </Button>
                <Button
                    x:Name="CleanButton"
                    ToolTipService.ToolTip="Eliminar toda la conversación"
                    Click="CleanButton_Click"
                    Visibility="Collapsed"
                    Padding="8" BorderThickness="0">
                    <Button.Content>
                        <FontIcon FontSize="18" Glyph="&#xEA99;" />
                    </Button.Content>
                </Button>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" x:Name="ChatGrid" MaxWidth="1200" VerticalAlignment="Center" SizeChanged="ItemsStackPanel_SizeChanged">
            <Grid.RowDefinitions>
                <RowDefinition x:Name="ChatRowDefinition" Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <Grid x:Name="Landing">
                <StackPanel
                    x:Name="LandingContent"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Vertical"
                    Spacing="8">
                    <Image Margin="0,0,0,16" HorizontalAlignment="Center" Height="150">
                        <Image.Source>
                            <BitmapImage DecodePixelHeight="150" UriSource="ms-appx:///Assets/Background/LandingChat.png" />
                        </Image.Source>
                    </Image>
                    <TextBlock
                        Style="{StaticResource TitleTextBlockStyle}"
                        Text="¡Hola! ¿En qué puedo ayudarte?"
                        TextTrimming="CharacterEllipsis"
                        TextWrapping="NoWrap"
                        HorizontalAlignment="Center"
                        Margin="0,0,0,18"/> 
                </StackPanel>
            </Grid>
            <ListView
                x:Name="InvertedListView"
                Padding="-12,0,-12,0"
                Margin="36,0,36,0"
                ItemTemplateSelector="{StaticResource ChatTemplateSelector}"
                ItemsSource="{x:Bind _chat.Messages}"
                SelectionMode="None"
                Loaded="InvertedListView_Loaded">
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsStackPanel VerticalAlignment="Bottom" ItemsUpdatingScrollMode="KeepLastItemInView" />
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>
            <lcontrols:ChatControl
                x:Name="ChatControl"
                Grid.Row="1"
                Margin="36,18,36,36"
                SendButtonClicked="ChatControl_SendButtonClicked"
                ParametersVisibilityChanged="ChatControl_ParametersVisibilityChanged"
                IsEnabled="{x:Bind _chat.ChatError, Mode=OneWay, Converter={StaticResource BoolNegationConverter}}" />
        </Grid>
    </Grid>
</local:EditorControl>
