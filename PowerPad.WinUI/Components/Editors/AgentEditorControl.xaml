<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="PowerPad.WinUI.Components.Editors.AgentEditorControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PowerPad.WinUI.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:PowerPad.WinUI.Converters"
    xmlns:toolkitconverters="using:CommunityToolkit.WinUI.Converters"
    mc:Ignorable="d">
    <UserControl.Resources>
        <converters:IntToDoubleConverter x:Key="IntToDoubleConverter"/>
        <converters:FloatToDoubleConverter x:Key="FloatToDoubleConverter"/>
        <toolkitconverters:StringFormatConverter x:Key="StringFormatConverter" />
    </UserControl.Resources>
    <Grid Padding="36,24,36,36" RowSpacing="36">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel MaxWidth="1000" HorizontalAlignment="Stretch">
            <TextBlock
                Style="{StaticResource TitleTextBlockStyle}"
                Text="Agente"/> 
        </StackPanel>
        <StackPanel Grid.Row="1" Spacing="16" MaxWidth="1000" HorizontalAlignment="Stretch" VerticalAlignment="Center">

            <Grid ColumnSpacing="32">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel Spacing="8">
                    <TextBlock Text="Icono" />
                    <Border 
                        BorderThickness="1" 
                        Height="100"
                        Width="100"
                        CornerRadius="50" 
                        BorderBrush="{ThemeResource ControlStrongStrokeColorDefaultBrush}">
                        <ContentControl
                            Content="{x:Bind _agent.IconElement}"
                            HorizontalAlignment="Left" />
                    </Border>
                    <Button
                        Content="Seleccionar"
                        Click="SaveButton_Click" />
                </StackPanel>

                <StackPanel Spacing="16" Grid.Column="1">
                    <TextBox
                        Header="Nombre"
                        Text="{x:Bind _agent.Name, Mode=TwoWay}"/>
                    <TextBox
                        Header="Descripción"
                        Text="{x:Bind _agent.Prompt, Mode=TwoWay}"
                        AcceptsReturn="True"
                        Height="150"
                        TextWrapping="Wrap" />
                </StackPanel>
            </Grid>

            <ToggleSwitch
                Margin="0,0,0,-16"
                OnContent="No incluir parámetro adiccional para la acción"
                OffContent="Incluir parámetro adiccional para la acción" />

            <Grid ColumnSpacing="16"
                Padding="16"
                BorderThickness="1"
                BorderBrush="{ThemeResource ControlStrongStrokeColorDefaultBrush}"
                CornerRadius="5"
                Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="2*" />
                </Grid.ColumnDefinitions>
                <TextBox
                    Header="Nombre"
                    Text="{x:Bind _agent.PromptParameterName, Mode=TwoWay}"/>
                <TextBox
                    Grid.Column="1"
                    Header="Descripción"
                    Text="{x:Bind _agent.PromptParameterDescription, Mode=TwoWay}"
                    Height="60"
                    TextWrapping="Wrap"/>
            </Grid>
                
             <Grid ColumnSpacing="16">
                 <Grid.ColumnDefinitions>
                     <ColumnDefinition Width="*" />
                     <ColumnDefinition Width="auto" />
                 </Grid.ColumnDefinitions>
                <TextBlock VerticalAlignment="Center" TextTrimming="CharacterEllipsis">
                        <Run Text="Modelo" />
                        <Run Text="(Si no está disponible se usará el modelo por defecto)"
                             Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                </TextBlock>
                <ComboBox
                    Grid.Column="1"
                    SelectedItem="{x:Bind _agent.AIModel, Mode=TwoWay}">
                </ComboBox>
             </Grid>

            <ToggleSwitch
                Margin="0,0,0,-16"
                OnContent="Enviar parámetros personalizados">
                <ToggleSwitch.OffContent>
                    <TextBlock>
                        <Run Text="No enviar parámetros personalizados" />
                        <Run Text="(Se enviarán parámetros por defecto)" Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                    </TextBlock>
                </ToggleSwitch.OffContent>
            </ToggleSwitch>

            <Grid ColumnSpacing="8" RowSpacing="8"
                Padding="16"
                BorderThickness="1"
                BorderBrush="{ThemeResource ControlStrongStrokeColorDefaultBrush}"
                CornerRadius="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="6*" />
                    <ColumnDefinition Width="6*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                
                <StackPanel 
                    Orientation="Horizontal" Spacing="8">
                    <TextBlock 
                        Text="Temperatura" 
                        VerticalAlignment="Center" />
                    <TextBlock 
                        Text="(nivel de aleatoriedad)" 
                        Foreground="{ThemeResource TextFillColorTertiaryBrush}" 
                        VerticalAlignment="Center" />
                </StackPanel>
                <Slider 
                    Grid.Column="1" 
                    Minimum="0" 
                    Maximum="1" 
                    StepFrequency="0.01" 
                    IsThumbToolTipEnabled="False"
                    Value="{x:Bind _agent.Temperature, Mode=TwoWay, Converter={StaticResource FloatToDoubleConverter}}"
                    IsEnabled="{x:Bind IsEnabled}"/>
                <TextBlock 
                    Grid.Column="2" 
                    VerticalAlignment="Center" 
                    TextAlignment="Left"
                    Width="28"
                    Text="{x:Bind _agent.Temperature, Mode=OneWay, Converter={StaticResource StringFormatConverter}, ConverterParameter='{}{0:0.00}'}" />

                <StackPanel 
                    Grid.Row="1"
                    Orientation="Horizontal" Spacing="8">
                    <TextBlock 
                        Text="Top P" 
                        VerticalAlignment="Center" />
                    <TextBlock 
                        Text="(núcleo de palabras candidatas)" 
                        Foreground="{ThemeResource TextFillColorTertiaryBrush}" 
                        VerticalAlignment="Center" />
                </StackPanel>
                <Slider 
                    Grid.Row="1" 
                    Grid.Column="1"
                    Minimum="0" 
                    Maximum="1" 
                    StepFrequency="0.01" 
                    IsThumbToolTipEnabled="False"
                    Value="{x:Bind _agent.TopP, Mode=TwoWay, Converter={StaticResource FloatToDoubleConverter}}"
                    IsEnabled="{x:Bind IsEnabled}"/>
                <TextBlock 
                    Grid.Row="1" 
                    Grid.Column="2" 
                    VerticalAlignment="Center" 
                    TextAlignment="Left"
                    Width="28"
                    Text="{x:Bind _agent.TopP, Mode=OneWay, Converter={StaticResource StringFormatConverter}, ConverterParameter='{}{0:0.00}'}" />

                <TextBlock 
                    Grid.Row="2" 
                    Text="Número máximo de tokens generados" 
                    VerticalAlignment="Center" />
                <NumberBox 
                    Grid.Row="2" 
                    Grid.Column="1" 
                    Grid.ColumnSpan="2" 
                    SpinButtonPlacementMode="Inline"
                    SmallChange="10"
                    LargeChange="100"
                    Minimum="10"
                    Value="{x:Bind _agent.MaxOutputTokens, Mode=TwoWay, Converter={StaticResource IntToDoubleConverter}}"
                    IsEnabled="{x:Bind IsEnabled}"/>
            </Grid>

            <StackPanel Orientation="Horizontal" Spacing="16">
                <ToggleSwitch
                    OnContent="Mostrar agente en notas"
                    OffContent="No mostrar agente en notas"
                    IsOn="{x:Bind _agent.ShowInNotes, Mode=TwoWay}"/>
                <ToggleSwitch
                    OnContent="Mostrar agente en chats"
                    OffContent="No mostrar agente en chats"
                    IsOn="{x:Bind _agent.ShowInChats, Mode=TwoWay}"/>
            </StackPanel>
            

            <StackPanel 
                Grid.Row="10" Grid.ColumnSpan="6" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
                <Button
                    x:Name="SaveButton"
                    IsEnabled="False"
                    Content="Guardar"
                    Click="SaveButton_Click"
                    HorizontalAlignment="Right"/>

                <Button
                    x:Name="CancelButton"
                    IsEnabled="False"
                    Content="Cancelar"
                    Click="CancelButton_Click"
                    HorizontalAlignment="Right"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
