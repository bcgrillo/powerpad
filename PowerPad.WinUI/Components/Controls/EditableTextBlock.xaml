<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="PowerPad.WinUI.Components.Controls.EditableTextBlock"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PowerPad.WinUI.Components.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    mc:Ignorable="d"
    PointerEntered="UserControl_PointerEntered"
    PointerExited="UserControl_PointerExited"
    GotFocus="UserControl_GotFocus"
    LostFocus="UserControl_LostFocus">
    <UserControl.Resources>
        <converters:BoolNegationConverter x:Key="BoolNegationConverter" />
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>
    <Grid ColumnSpacing="8" Background="Transparent">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <Border
            x:Name="Border"
            CornerRadius="5"
            BorderThickness="1"
            Margin="-6,0,0,0"
            BorderBrush="{x:Bind _state.BorderBrush, Mode=OneWay}">
            <local:IntegratedTextBox 
                x:Name="IntegratedTextBox"
                IsReadOnly="{x:Bind _state.IsEditing, Mode=OneWay, Converter={StaticResource BoolNegationConverter}}"
                KeyDown="IntegratedTextBox_KeyDown"
                LostFocus="IntegratedTextBox_LostFocus"
                Padding="6"
                TextWrapping="Wrap"/>
        </Border>
        <Button 
            x:Name="EditButton"
            Grid.Column="1" 
            Click="Button_Click" 
            ToolTipService.ToolTip="Editar"
            Background="Transparent"
            BorderBrush="Transparent"
            Opacity="0"
            Visibility="{x:Bind _state.IsEditing, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=True}">
            <FontIcon Glyph="&#xE70F;" />
        </Button>
        <StackPanel
            Grid.Column="1" 
            Orientation="Horizontal"
            Spacing="8"
            Visibility="{x:Bind ConfirmOnLostFocus, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=True}">
            <Button 
                x:Name="ConfirmButton"
                Click="Button_Click"
                ToolTipService.ToolTip="Confirmar"
                Background="Transparent"
                BorderBrush="Transparent"
                Visibility="{x:Bind _state.IsEditing, Mode=OneWay}">
                <FontIcon Glyph="&#xE8FB;" />
            </Button>
            <Button 
                x:Name="CancelButton"
                Click="Button_Click"
                ToolTipService.ToolTip="Cancelar"
                Background="Transparent"
                BorderBrush="Transparent"
                Visibility="{x:Bind _state.IsEditing, Mode=OneWay}">
                <FontIcon Glyph="&#xE711;" />
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
