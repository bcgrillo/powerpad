<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="PowerPad.WinUI.Components.Controls.ChatControlParameters"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PowerPad.WinUI.Components.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:PowerPad.WinUI.Converters"
    mc:Ignorable="d">
    <UserControl.Resources>
        <converters:IntToDoubleConverter x:Key="IntToDoubleConverter"/>
        <converters:FloatToDoubleConverter x:Key="FloatToDoubleConverter"/>
    </UserControl.Resources>
    <StackPanel VerticalAlignment="Stretch" Orientation="Vertical" Spacing="16">
        <TextBox 
            x:Name="SystemPromptTextBox"
            Header="Instrucciones de sistema"
            AcceptsReturn="True"
            TextWrapping="Wrap"
            Height="100"
            Text="{x:Bind Parameters.SystemPrompt, Mode=TwoWay}"
            IsEnabled="{x:Bind IsEnabled}"/>
        
        <Grid ColumnSpacing="16" RowSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            
            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBlock 
                    Text="Temperatura" 
                    VerticalAlignment="Center" />
                <TextBlock 
                    Text="(nivel de aleatoriedad)" 
                    Foreground="{ThemeResource TextFillColorTertiaryBrush}" 
                    VerticalAlignment="Center" />
            </StackPanel>
            <Slider Grid.Column="1" 
                Minimum="0" 
                Maximum="1" 
                StepFrequency="0.01" 
                IsThumbToolTipEnabled="False"
                Value="{x:Bind Parameters.Temperature, Mode=TwoWay, Converter={StaticResource FloatToDoubleConverter}}"
                IsEnabled="{x:Bind IsEnabled}"/>
            <TextBlock 
                Grid.Column="2" 
                VerticalAlignment="Center" 
                HorizontalAlignment="Right"
                Text="{x:Bind Parameters.Temperature, Mode=OneWay}" />
            
            <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8">
                <TextBlock 
                    Text="Top P" 
                    VerticalAlignment="Center" />
                <TextBlock 
                    Text="(nucleo de palabras candidatas)" 
                    Foreground="{ThemeResource TextFillColorTertiaryBrush}" 
                    VerticalAlignment="Center" />
            </StackPanel>
            <Slider Grid.Row="1" Grid.Column="1"
                Minimum="0" 
                Maximum="1" 
                StepFrequency="0.01" 
                IsThumbToolTipEnabled="False"
                Value="{x:Bind Parameters.TopP, Mode=TwoWay, Converter={StaticResource FloatToDoubleConverter}}"
                IsEnabled="{x:Bind IsEnabled}"/>
            <TextBlock 
                Grid.Row="1" 
                Grid.Column="2" 
                VerticalAlignment="Center" 
                HorizontalAlignment="Right" 
                Text="{x:Bind Parameters.TopP, Mode=OneWay}" />
            
            <TextBlock 
                Grid.Row="2" 
                Text="Número máximo de tokens generados" 
                VerticalAlignment="Center" />
            <NumberBox 
                Grid.Row="2" 
                Grid.Column="1" 
                Grid.ColumnSpan="2" 
                SpinButtonPlacementMode="Inline"
                SmallChange="10"
                LargeChange="100"
                Value="{x:Bind Parameters.MaxOutputTokens, Mode=TwoWay, Converter={StaticResource IntToDoubleConverter}}"
                IsEnabled="{x:Bind IsEnabled}"/>

            <TextBlock 
                Grid.Row="3" 
                Text="Tamaño máximo de la conversación" 
                VerticalAlignment="Center" />
            <NumberBox 
                Grid.Row="3" 
                Grid.Column="1" 
                Grid.ColumnSpan="2" 
                SpinButtonPlacementMode="Inline"
                SmallChange="1"
                LargeChange="10"
                Value="{x:Bind Parameters.MaxConversationLength, Mode=TwoWay, Converter={StaticResource IntToDoubleConverter}}"
                IsEnabled="{x:Bind IsEnabled}"/>
        </Grid>
    </StackPanel>
</UserControl>
