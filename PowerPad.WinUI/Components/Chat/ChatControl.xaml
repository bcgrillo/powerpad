<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="PowerPad.WinUI.Components.Controls.ChatControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PowerPad.WinUI.Components.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:tkcontrols="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">
    <UserControl.Resources>
        <x:Boolean x:Key="TrueValue">True</x:Boolean>
        <x:Boolean x:Key="FalseValue">False</x:Boolean>
    </UserControl.Resources>
    <tkcontrols:SwitchPresenter Value="{x:Bind _settings.IsAIAvailable, Mode=TwoWay}">
        <tkcontrols:Case Value="{StaticResource TrueValue}">
            <StackPanel 
                Orientation="Vertical" 
                Spacing="4"
                Margin="0,0,0,-24">
                <Grid
                    x:Name="ParametersPanel"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    Padding="16"
                    CornerRadius="10"
                    Visibility="Collapsed" >
                    <StackPanel VerticalAlignment="Stretch" Orientation="Vertical" Spacing="16">
                        <Grid 
                            HorizontalAlignment="Stretch" 
                            VerticalAlignment="Center">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <ToggleSwitch 
                                x:Name="EnableParametersSwitch" 
                                VerticalAlignment="Center"
                                Margin="0,-6,-8,-6"
                                MinWidth="0"
                                Toggled="EnableParametersSwitch_Toggled">
                                <ToggleSwitch.OnContent>
                                    <TextBlock 
                                        Text="Enviar parámetros personalizados" 
                                        Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                </ToggleSwitch.OnContent>
                                <ToggleSwitch.OffContent>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <TextBlock 
                                            Text="No enviar parámetros personalizados" 
                                            Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                        <TextBlock 
                                            Text="(Se envían parámetros predeterminados)" 
                                            Visibility="{x:Bind _settings.Models.SendDefaultParameters, Mode=OneWay}"
                                            Foreground="{ThemeResource TextFillColorTertiaryBrush}" 
                                            VerticalAlignment="Center" />
                                    </StackPanel>
                                </ToggleSwitch.OffContent>
                            </ToggleSwitch>
                            <Button 
                                x:Name="CloseParametersButton"
                                Grid.Column="2" HorizontalAlignment="Right"
                                Background="Transparent"
                                BorderThickness="0" Padding="8"
                                Click="CloseParametersButton_Click">
                                <FontIcon FontSize="14" Glyph="&#xE8BB;" 
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            </Button>
                        </Grid>
                        <local:ChatControlParameters x:Name="ControlDefaultParamters" Parameters="{x:Bind _settings.Models.DefaultParameters}" Visibility="Visible" IsEnabled="False" />
                        <local:ChatControlParameters x:Name="ControlCustomParamters" Parameters="{x:Bind _parameters}" Visibility="Collapsed" />
                    </StackPanel>
                </Grid>
                <Grid
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    Padding="16"
                    CornerRadius="10"
                    RowSpacing="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <local:IntegratedTextBox
                        x:Name="ChatInputBox"
                        PlaceholderText="Escribe lo que quieras..."
                        TextWrapping="Wrap" 
                        MaxHeight="148"
                        ScrollViewer.VerticalScrollBarVisibility="Auto"
                        Padding="0"
                        FontSize="15"
                        KeyDown="ChatInputBox_KeyDown"
                        TextChanged="ChatInputBox_TextChanged"/>
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button x:Name="AgentToggleButton" Click="AgentToggleButton_Click" Height="32" Padding="0" ToolTipService.ToolTip="User agentes">
                                <Border VerticalAlignment="Stretch" Height="28" Margin="1" Padding="6,4" BorderBrush="Transparent" BorderThickness="1" CornerRadius="5">
                                    <FontIcon Glyph="&#xE99A;" FontSize="16" />
                                </Border>
                            </Button>
                            <local:AgentSelector x:Name="AgentSelector" DocumentType="Chat" Visibility="Collapsed" />
                            <local:ModelSelector x:Name="ModelSelector" />
                            <ToggleButton x:Name="ParametersButton" Click="ParametersButton_Click" Height="32" Padding="0" ToolTipService.ToolTip="Parámetros">
                                <Border VerticalAlignment="Stretch" Height="28" Margin="1" Padding="6,4" BorderBrush="Transparent" BorderThickness="1" CornerRadius="5">
                                    <local:ButtonIcon x:Name="ParametersIcon" Source="{ThemeResource SettingsSmallSvg}" HorizontalAlignment="Stretch" />
                                </Border>
                            </ToggleButton>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button x:Name="SendButton"
                                Click="SendBtn_Click"
                                Style="{StaticResource AccentButtonStyle}"
                                Height="32"
                                BorderThickness="0" Padding="8"
                                IsEnabled="False">
                                <StackPanel Orientation="Horizontal">
                                    <FontIcon Margin="0,1,-1,0" FontSize="16" Glyph="&#xE725;" />
                                </StackPanel>
                            </Button>
                            <Button x:Name="StopButton"
                                Style="{ThemeResource AccentButtonStyle}"
                                Visibility="Collapsed"
                                Height="32"
                                BorderThickness="0" Padding="8"
                                Click="StopBtn_Click">
                                <StackPanel Orientation="Horizontal">
                                    <FontIcon Margin="0,0,-1,1" FontSize="16" Glyph="&#xF140;" />
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Grid>
                <TextBlock 
                    Text="El contenido generado por inteligencia artificial puede contener errores." 
                    VerticalAlignment="Stretch"
                    TextAlignment="Center"
                    Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                    FontSize="12"
                    Height="18" />
            </StackPanel>
        </tkcontrols:Case>
        <tkcontrols:Case Value="{StaticResource FalseValue}">
            <muxc:InfoBar 
                IsOpen="True"
                Severity="Informational"
                Content="No hay modelos disponibles, revise la configuración."
                IsClosable="False" />
        </tkcontrols:Case>
        <tkcontrols:Case IsDefault="True">
            <StackPanel
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                Padding="16"
                CornerRadius="10">
                <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
                    <muxc:ProgressRing Height="20" Width="20" />
                    <TextBlock  VerticalAlignment="Center"
                    Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    FontSize="16"
                    Text="Inicializando..." />
                </StackPanel>
            </StackPanel>
        </tkcontrols:Case>
    </tkcontrols:SwitchPresenter>
</UserControl>
