<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="PowerPad.WinUI.Components.WorkspaceControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PowerPad.WinUI.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="using:PowerPad.Core.Models"
    mc:Ignorable="d">
    <Grid>
        <TreeView Name="TreeView" ItemsSource="{x:Bind FileManager.WorkspaceItems}" ItemInvoked="TreeView_ItemInvoked">
            <TreeView.ItemTemplate>
                <DataTemplate x:DataType="model:FileItem">
                    <TreeViewItem ItemsSource="{x:Bind Children}" Content="{x:Bind Name}" Padding="0 0 10 0"/>
                </DataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </Grid>
</UserControl>
