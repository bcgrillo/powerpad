<?xml version="1.0" encoding="utf-8"?>
<local:DisposablePage
    x:Class="PowerPad.WinUI.Pages.AgentsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PowerPad.WinUI.Pages"
    xmlns:controls="using:PowerPad.WinUI.Components.Controls"
    xmlns:components="using:PowerPad.WinUI.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:vm="using:PowerPad.WinUI.ViewModels.Agents"
    mc:Ignorable="d">
    <Grid Margin="0,0,0,2">
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid x:Name="AgentsMenu" Grid.RowSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="48"/>
                <RowDefinition Height="*" />
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <TextBlock
                Style="{StaticResource WSTitleStyle}"
                Text="Agentes"/>
            <ScrollViewer Width="260" Grid.Row="1" VerticalScrollMode="Auto" HorizontalScrollMode="Auto">
                <TreeView 
                    Name="TreeView" 
                    ItemsSource="{x:Bind _agentsCollection.Agents, Mode=OneWay}" 
                    ItemInvoked="TreeView_ItemInvoked"
                    CanDrag="False"
                    CanReorderItems="True">
                    <TreeView.ItemTemplate>
                        <DataTemplate x:DataType="vm:AgentViewModel">
                            <TreeViewItem 
                                Padding="0,0,8,0"
                                AllowDrop="{x:Bind AllowDropFalse}">
                                <StackPanel Padding="3" Orientation="Horizontal">
                                    <controls:AgentIconControl
                                        IsEnabled="{x:Bind Enabled, Mode=OneWay}"
                                        Margin="-30,2,10,0" Size="16" AgentIcon="{x:Bind Icon, Mode=OneWay}" />
                                    <controls:Label
                                        IsEnabled="{x:Bind Enabled, Mode=OneWay}"
                                        Margin="0,2,8,-2" Text="{x:Bind Name, Mode=OneWay}"/>
                                </StackPanel>
                            </TreeViewItem>
                        </DataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </ScrollViewer>
            <Grid Grid.Row="2" Margin="4,8,4,4">
                <Button 
                    x:Name="NewAgentButton"
                    Height="36"
                    HorizontalAlignment="Stretch"
                    BorderThickness="0"
                    Click="NewAgentButton_Click"
                    ToolTipService.ToolTip="Nuevo agente">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon FontSize="16" Glyph="&#xE710;" />
                        <TextBlock Text="Añadir agente" />
                    </StackPanel>
                </Button>
            </Grid>
        </Grid>

        <Grid
            Grid.Row="1"
            Grid.Column="1"
            Background="{ThemeResource PowerPadEditorBrush}"
            CornerRadius="10,0,0,0">
            <StackPanel 
                x:Name="AgentLanding" 
                HorizontalAlignment="Center" 
                VerticalAlignment="Center"
                Spacing="8">
                <Image Margin="0,0,0,16" HorizontalAlignment="Center" Height="200">
                    <Image.Source>
                        <BitmapImage DecodePixelHeight="200" UriSource="ms-appx:///Assets/Background/LandingAgents.png" />
                    </Image.Source>
                </Image>
                <TextBlock
                    Style="{StaticResource SubtitleTextBlockStyle}"
                    Text="Seleccione un agente para modificarlo o cree uno nuevo."
                    TextTrimming="CharacterEllipsis"
                    TextWrapping="NoWrap"
                    HorizontalAlignment="Center"
                    Margin="0,0,0,16"/>
                <Button 
                    Height="36"
                    BorderThickness="0"
                    Click="NewAgentButton_Click"
                    HorizontalAlignment="Center"
                    ToolTipService.ToolTip="Nuevo agente"
                    Style="{StaticResource AccentButtonStyle}">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <FontIcon FontSize="16" Glyph="&#xE710;" />
                        <TextBlock Text="Crear un nuevo agente" />
                    </StackPanel>
                </Button>
            </StackPanel>
            <Grid 
                Visibility="Collapsed"
                x:Name="AgentEditorContent" />
        </Grid>
    </Grid>
</local:DisposablePage>