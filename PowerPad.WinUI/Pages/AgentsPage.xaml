<?xml version="1.0" encoding="utf-8"?>
<local:DisposablePage
    x:Class="PowerPad.WinUI.Pages.AgentsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PowerPad.WinUI.Pages"
    xmlns:controls="using:PowerPad.WinUI.Components.Controls"
    xmlns:components="using:PowerPad.WinUI.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:vm="using:PowerPad.WinUI.ViewModels.Agents"
    mc:Ignorable="d">
    <Grid Margin="0,0,0,2">
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid x:Name="AgentsMenu" Grid.RowSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="48"/>
                <RowDefinition Height="*" />
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>
                <TextBlock
                    Style="{StaticResource WSTitleStyle}"
                    Text="Agentes"/>
                <Button
                    x:Name="HideMenuBtn"
                    Grid.Column="2"
                    Height="30"
                    Width="30"
                    ToolTipService.ToolTip="Ocultar menu"
                    Margin="0,0,10,0"
                    Padding="6,5,6,6"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Click="HideMenuBtn_Click"
                    Canvas.ZIndex="1"
                    IsHitTestVisible="True">
                    <Button.Content>
                        <controls:ButtonIcon Source="{ThemeResource HideMenuSvg}" />
                    </Button.Content>
                </Button>
            </Grid>
            <ScrollViewer Width="260" Grid.Row="1" VerticalScrollMode="Auto" HorizontalScrollMode="Auto">
                <TreeView 
                    Name="TreeView" 
                    ItemsSource="{x:Bind _agentsCollection.Agents, Mode=OneWay}" 
                    ItemInvoked="TreeView_ItemInvoked"
                    CanDrag="False"
                    CanReorderItems="True">
                    <!--DragItemsCompleted="TreeView_DragItemsCompleted">-->
                    <TreeView.ItemTemplate>
                        <DataTemplate x:DataType="vm:AgentViewModel">
                            <TreeViewItem 
                                Padding="0,0,8,0"
                                AllowDrop="{x:Bind AllowDropFalse}">
                                <StackPanel Padding="3" Orientation="Horizontal">
                                    <ContentControl Margin="-30,2,10,0" Height="20" Width="20" HorizontalAlignment="Center" Content="{x:Bind IconElement, Mode=OneWay}" />
                                    <TextBlock Margin="0,2,8,-2" Text="{x:Bind Name, Mode=OneWay}"/>
                                </StackPanel>
                            </TreeViewItem>
                        </DataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </ScrollViewer>
            <Grid Grid.Row="2" Margin="4,8,4,4">
                <Button 
                    x:Name="NewAgentButton"
                    Height="36"
                    HorizontalAlignment="Stretch"
                    BorderThickness="0"
                    Click="NewAgentButton_Click"
                    ToolTipService.ToolTip="Nuevo agente">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon FontSize="16" Glyph="&#xE710;" />
                        <TextBlock Text="Añadir agente" />
                    </StackPanel>
                </Button>
            </Grid>
        </Grid>

        <Border
            Grid.Row="1"
            Grid.Column="1"
            Background="{ThemeResource PowerPadEditorColor}"
            CornerRadius="10,0,0,0">
            <Frame x:Name="AgentsFrame" />
        </Border>
    </Grid>
</local:DisposablePage>