<?xml version="1.0" encoding="utf-8"?>
<local:AIAddModelPageBase
    x:Class="PowerPad.WinUI.Pages.Providers.OllamaAddModelPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PowerPad.WinUI.Pages.Providers"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:ai="using:PowerPad.WinUI.ViewModels.AI"
    xmlns:components="using:PowerPad.WinUI.Components"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    mc:Ignorable="d"
    Background="{ThemeResource PowerPadEditorColor}">
    <Page.Resources>
        <converters:BoolNegationConverter x:Key="BoolNegationConverter" />
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition x:Name="RowHeader" Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Margin="36,36,36,36" VerticalAlignment="Center" Spacing="24">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="20">
                <Image Source="{ThemeResource OllamaSvg}" Width="42" Height="42" />
                <TextBlock
                    Style="{StaticResource TitleTextBlockStyle}"
                    Text="Buscar modelos en la biblioteca de Ollama" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBox 
                    x:Name="SearchTextBox"
                    Width="300" 
                    Height="32" 
                    PlaceholderText="Nombre del modelo" 
                    Margin="0,0,6,0"
                    KeyDown="Search_KeyDown"
                    PlaceholderForeground="{ThemeResource TextFillColorTertiaryBrush}"
                    IsEnabled="{x:Bind _settings.General.OllamaConfig.HasError, Mode=OneWay, Converter={StaticResource BoolNegationConverter}}" />
                <Button
                    Height="32"
                    ToolTipService.ToolTip="Buscar"
                    Click="Search_Click"
                    IsEnabled="{x:Bind _settings.General.OllamaConfig.HasError, Mode=OneWay, Converter={StaticResource BoolNegationConverter}}">
                    <Button.Content>
                        <FontIcon Glyph="&#xE721;" FontSize="14" />
                    </Button.Content>
                </Button>
            </StackPanel>
            <muxc:InfoBar 
                IsOpen="{x:Bind _settings.General.OllamaConfig.HasError, Mode=OneWay}"
                IsClosable="False"
                Content="Error al conectar a Ollama, revise la configuración." />
        </StackPanel>
        
        <Grid 
            Grid.Row="1">
            <components:SearchModelsResultRepeater
                x:Name ="SearchModelsResultRepeater"
                SearchingFlag="{x:Bind _modelsViewModel.Searching, Mode=OneWay}"
                Models="{x:Bind _modelsViewModel.SearchResultModels, Mode=OneWay}"
                AddModelClick="AddModel_Click"
                ModelInfoViewerVisibilityChanged="SearchModelsResultRepeater_ModelInfoViewerVisibilityChanged"
                SearchEmpty="{x:Bind _modelsViewModel.SearchResultModelsEmpty, Mode=OneWay}" />
        </Grid>
    </Grid>
</local:AIAddModelPageBase>