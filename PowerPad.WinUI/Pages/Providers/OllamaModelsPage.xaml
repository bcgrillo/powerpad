<?xml version="1.0" encoding="utf-8"?>
<local:AIModelsPageBase
    x:Class="PowerPad.WinUI.Pages.Providers.OllamaModelsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PowerPad.WinUI.Pages.Providers"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:ai="using:PowerPad.WinUI.ViewModels.AI"
    xmlns:components="using:PowerPad.WinUI.Components"
    xmlns:converters="using:PowerPad.WinUI.Converters"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource PowerPadEditorBrush}">
    <local:AIModelsPageBase.Resources>
        <converters:ServiceStatusToBoolConverter x:Key="ServiceStatusToBoolConverter" />
        <converters:ServiceStatusToColorBrushConverter x:Key="ServiceStatusToColorBrushConverter" />
        <converters:ServiceStatusToStringConverter x:Key="ServiceStatusToStringConverter" />
        <converters:ServiceStatusToBoolNegationConverter x:Key="ServiceStatusToBoolNegationConverter" />
    </local:AIModelsPageBase.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition x:Name="RowHeader" Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Margin="36,36,36,20" RowSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <Image Grid.Column="0" Source="{ThemeResource OllamaSvg}" Width="42" Height="42" Margin="0,0,24,0"/>
            <TextBlock
                Grid.Column="1"
                Style="{StaticResource TitleTextBlockStyle}"
                Text="Modelos de Ollama" />
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <TextBlock Height="32" Margin="0,6,6,-6" Foreground="{ThemeResource TextFillColorSecondaryBrush}">Estado Ollama:</TextBlock>
                <TextBlock 
                    Height="32" 
                    Margin="0,6,0,-6" 
                    Foreground="{x:Bind _settings.General.OllamaConfig.ServiceStatus, Mode=OneWay, Converter={StaticResource ServiceStatusToColorBrushConverter}}"
                    Text="{x:Bind _settings.General.OllamaConfig.ServiceStatus, Mode=OneWay, Converter={StaticResource ServiceStatusToStringConverter}}" />
                <Button
                    Height="32"
                    ToolTipService.ToolTip="Refrescar"
                    Margin="12,0,0,0"
                    Command="{x:Bind _ollamaModelsViewModel.RefreshModelsCommand}"
                    IsEnabled="{x:Bind _settings.General.OllamaConfig.ServiceStatus, Mode=OneWay, Converter={StaticResource ServiceStatusToBoolConverter}}">
                    <Button.Content>
                        <FontIcon Glyph="&#xE72C;" FontSize="14" />
                    </Button.Content>
                </Button>
            </StackPanel>
            <muxc:InfoBar 
                Grid.Row="1"
                Grid.ColumnSpan="3"
                IsOpen="{x:Bind _settings.General.OllamaConfig.ServiceStatus, Mode=OneWay, Converter={StaticResource ServiceStatusToBoolNegationConverter}}"
                IsClosable="False"
                Severity="Warning"
                Content="Ollama no disponible, revise la configuración." />
        </Grid>

        <components:AvailableModelsRepeater
            x:Name ="AvailableModelsRepeater"
            Grid.Row="1"
            Models="{x:Bind _modelsViewModel.FilteredModels, Mode=OneWay}"
            DeleteClick="Delete_Click"
            SetDefaultClick="SetDefault_Click"
            ModelInfoViewerVisibilityChanged="AIModelsRepeater_ModelInfoViewerVisibilityChanged"
            AddButtonClick="AvailableModelsRepeater_AddButtonClick"
            ModelsEmpty="{x:Bind _modelsViewModel.FilteredModelsEmpty, Mode=OneWay}"
            IsEnabled="{x:Bind _modelsViewModel.RepeaterEnabled, Mode=OneWay}" />
    </Grid>
</local:AIModelsPageBase>