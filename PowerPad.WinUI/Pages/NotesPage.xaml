<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PowerPad.WinUI.Pages.NotesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PowerPad.WinUI.Pages"
    xmlns:components="using:PowerPad.WinUI.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:CommunityToolkit.WinUI"
    mc:Ignorable="d">
    <Grid ColumnDefinitions="auto, *">
        <components:WorkspaceControl 
            x:Name="WorkspaceControl"
            Grid.Column="0"
            ItemInvoked="WorkspaceControl_ItemInvoked"
            VisibilityChanged="WorkspaceControl_VisibilityChanged"/>
        <Border
            Grid.Column="1"
            Background="{ThemeResource SmokeFillColorDefaultBrush}">
            <components:EditorManager 
                x:Name="EditorManager"/>
        </Border>
    </Grid>
</Page>