<ContentDialog
    x:Class="PowerPad.WinUI.Dialogs.OllamaDownloadHelper"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="ContentDialog"
    PrimaryButtonClick="OnPrimaryButtonClick"
    Style="{ThemeResource DefaultContentDialogStyle}">
    <ContentDialog.Resources>
        <x:Double x:Key="ContentDialogMaxWidth">700</x:Double>
        <x:Double x:Key="ContentDialogMinWidth">700</x:Double>
    </ContentDialog.Resources>
    <StackPanel Orientation="Vertical" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <Grid HorizontalAlignment="Stretch" ColumnSpacing="16" Margin="0,16,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ImageIcon Height="36" VerticalAlignment="Center" Source="{ThemeResource OllamaSvg}"/>
            <TextBlock x:Name="Message" Grid.Column="1" TextWrapping="Wrap" HorizontalAlignment="Stretch" 
                Text="Parece que Ollama no está instalado en su equipo.&#10;Necesita Ollama para ejecutar modelos de inteligencia artificial localmente.&#10;¿Quiere descargarlo e instalarlo ahora?" />
        </Grid>
        <TextBlock x:Name="MessageAux" Grid.Column="1" TextWrapping="Wrap" HorizontalAlignment="Stretch" Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Text="También puede utilizar PowerPad con inteligencia artificial en la nube con Azure AI y OpenAI, revise la configuración para obtener o establecer una clave de servicio gratuita o de pago por uso." />
        <ProgressBar x:Name="ProgressBar" Margin="0,16,0,0" Visibility="Collapsed" />
    </StackPanel>
</ContentDialog>
